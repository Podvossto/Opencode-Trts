// Purpose: Application form builder — create and manage dynamic application forms per job
// Ref: API Contract — GET /api/v1/forms, POST /api/v1/forms
// DB table: application_forms, form_fields
// Dev must implement: FormBuilder component, field type palette (text/select/file/date), preview
export default function FormsPage() {
  return (
    <div>
      {/* TODO: Implement FormBuilder — GET/POST /api/v1/forms */}
      <p>Form Builder Page</p>
    </div>
  )
}
