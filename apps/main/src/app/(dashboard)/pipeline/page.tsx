// Purpose: Recruitment pipeline overview — kanban-style view of all applications by stage
// Ref: API Contract — GET /api/v1/pipeline
// DB table: applications, pipeline_actions
// Dev must implement: PipelineBoard component, column per stage, drag-to-action, filters by job
export default function PipelinePage() {
  return (
    <div>
      {/* TODO: Implement PipelineBoard — GET /api/v1/pipeline */}
      <p>Pipeline Page</p>
    </div>
  )
}
