// Purpose: System setup page — departments, email templates, system-level configuration
// Ref: API Contract — GET/POST /api/v1/setup/departments, /api/v1/setup/email-templates
// DB table: departments, email_templates, system_settings
// Dev must implement: SetupTabs (Departments | Email Templates | Settings), CRUD for each section
export default function SetupPage() {
  return (
    <div>
      {/* TODO: Implement SetupTabs — GET/POST /api/v1/setup/* */}
      <p>Setup Page</p>
    </div>
  )
}
