// Purpose: Job positions listing page — browse, create, publish/unpublish job postings
// Ref: API Contract — GET /api/v1/jobs, POST /api/v1/jobs
// DB table: jobs
// Dev must implement: JobList component, status filter (draft/active/closed), pagination
export default function JobsPage() {
  return (
    <div>
      {/* TODO: Implement JobList — GET/POST /api/v1/jobs */}
      <p>Jobs Page</p>
    </div>
  )
}
