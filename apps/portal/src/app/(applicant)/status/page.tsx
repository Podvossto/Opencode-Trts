// Purpose: Applicant status tracker page — authenticated applicant views pipeline progress
// Ref: API Contract — GET /api/v1/applicant/applications (Bearer JWT — Applicant role)
// DB table: applications, pipeline_actions
// Dev must implement: ApplicationStatusTimeline, stage badges (Resume Review / Test / Interview / Hire/Drop)
export default function ApplicantStatusPage() {
  return (
    <div>
      {/* TODO: Implement ApplicationStatusTimeline — GET /api/v1/applicant/applications */}
      <p>Application Status Page</p>
    </div>
  )
}
